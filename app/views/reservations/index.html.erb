<h1>My reservations</h1>
<% @reservations.each do |reservation| %>
  <div>
    <%= reservation.vehicle.name %>
    from <%= reservation.start_date %> to <%= reservation.end_date %>
    pour la modique somme de <%= reservation.total_price %>
  </div>
<% end %>
